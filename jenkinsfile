pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Clone Successful'
            }
        }

        stage('Build') {
            steps {
                sh 'echo Build Successful'
            }
        }

        stage('Deploy') {
            steps {
                sh 'echo Deployment Successful'
            }
        }
    }
}
